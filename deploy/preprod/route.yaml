apiVersion: cirrus.ibm.com/v1alpha1
kind: Route
metadata:
  name: my-app-pipeline-preprod-route
spec:
  to:
    kind: Application
    name: my-app-pipeline-preprod
  port:
    targetPort: https
  tls:
    termination: passthrough
  host: my-app-pipeline-preprod.dal.cirrus.ibm.com
