apiVersion: cirrus.ibm.com/v1alpha1
kind: Route
metadata:
  name: zeus-server-test-route
spec:
  to:
    kind: Application
    name: zeus-server-test
  port:
    targetPort: https
  tls:
    termination: passthrough
  host: zeus-server-test.dal.cirrus.ibm.com
